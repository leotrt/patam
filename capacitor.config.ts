import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ailete.patam',
    appName: 'Patam',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
